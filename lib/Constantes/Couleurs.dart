import 'package:flutter/material.dart';

class Couleurs {
  // =========================
  // 🇨🇮 COULEURS PRINCIPALES
  // =========================

  // Vert principal (inspiration Côte d'Ivoire 🌿)
  static const Color primaryGreen = Color(0xFF1B8A5A);

  // Vert clair (background / badges)
  static const Color lightGreen = Color(0xFFE8F5E9);

  // Vert foncé (headers / boutons actifs)
  static const Color darkGreen = Color(0xFF0B5D3B);

  // =========================
  // 🟠 COULEURS SECONDAIRES
  // =========================

  // Orange ivoirien (accent)
  static const Color accentOrange = Color(0xFFF57C00);

  // Orange clair
  static const Color lightOrange = Color(0xFFFFE0B2);

  // =========================
  // 🔵 COULEURS UTILITAIRES
  // =========================

  // Bleu (localisation / carte)
  static const Color mapBlue = Color(0xFF1976D2);

  // Rouge (urgence / pharmacie de garde)
  static const Color emergencyRed = Color(0xFFD32F2F);

  // =========================
  // ⚪ NEUTRES
  // =========================

  static const Color white = Colors.white;

  static const Color background = Color(0xFFF9FAFB);

  static const Color lightGrey = Color(0xFFE0E0E0);

  static const Color grey = Color(0xFF9E9E9E);

  static const Color darkText = Color(0xFF212121);

  static const Color lightText = Color(0xFF757575);

  // =========================
  // 🎯 SPÉCIFIQUE APP PHARMACIE
  // =========================

  // Pharmacie ouverte
  static const Color openPharmacy = Color(0xFF2ECC71);

  // Pharmacie fermée
  static const Color closedPharmacy = Color(0xFFB0BEC5);

  // Badge "de garde"
  static const Color garde = Color(0xFFFF7043);

  // Fond carte
  static const Color mapBackground = Color(0xFFE3F2FD);
}